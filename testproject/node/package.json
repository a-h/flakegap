{
  "name": "nodetestproject",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --bundle index.ts --outdir=build --minify --sourcemap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
